# D01：真实取数基线与现场对照

总控 #46；本阶段 #47。当前仅补诊断和访问契约，不表示三站取得正文。
实施读取的主干为 `108941f2e1bb64c032170aa2d178aa5a38e3b58d`（PR44 已合并）。

## 已确认的主干事实

| 项目 | 当前事实 | 不得外推的结论 |
|---|---|---|
| PR44 | 重复请求头修复已合并 | 不是三站全部故障的唯一已证实根因 |
| #25 | 交付清单记录 PR32 已纳入有限匿名 SOCKS5 | 不代表认证、PAC、VM或所有网络支持 |
| #27 | 交付清单记录 PR31 已合并严格发布方节奏/有限恢复等待 | 不重复实现；不意味着长期任务检查点完成 |
| #28 | 主干交付清单记录CI执行/源码验收已交付 | 新 PR 仍须绑定新 head 重新运行；旧根因未知 |
| 三站 | #45 保留正文认证和实际失败来源的未完成项 | 受控动态页面和CI绿色不能替代实站 |

规划记录中的猎聘现象是无样式页面及 robots_unavailable；BOSS 是页面壳和 network_error；
51job 的固定 robots 观察是200/HTML。它们是之前环境和时间的观察，不是本次新抓包。
尤其不能把固定观察任务未请求列表/详情解释为已经完成平台自动采集尝试。

## 本轮证据分类

1. **代码/离线测试**：新增元数据观察器、原入口接线、错误分类、脱敏、有界缓存、
   同一任务到报告关联、本地HTTP接口令牌/同源验证。测试上游全部人工构造。
2. **真实浏览器受控测试**：在既有 `run_guided_browser.py` 增加勾选、预览、下载、关闭验证；
   在 `run_browser_policy_binding.py` 增加真实回调 → HTTP桥 → 解析/落盘/报告的 trace 验证。
   同一脚本继续保留原有的 TLS/DoH/撤销/头字段与样式断言。
3. **用户现场**：本开发会话没有连接用户电脑或招聘登录状态；
   普通浏览器与旧桥的同机同网对照尚未执行。不写成“已复现”或“已排除”。
4. **原生后端对照**：尚待 #48，实现前不伪造第三组结果。

具体测试数量、源码指纹、CI运行和本执行环境限制写入本PR，而不是在本文件提前标通过。

## 合并后如何采集本地证据

停止旧服务并更新到用户审核合并后的源码，保留原工作区、解释器、浏览器及网络偏好。
无需新增依赖，不重装 Edge/Playwright，不删除配额。

在 `/guided` 新建任务时勾选“本次启用脱敏取数诊断”，或选中已有任务后，
在空闲状态点“启用本任务诊断”。仅启用不会重试请求或补回过去的事件。
继续原本获准的一次操作后，点“本地预览诊断”。核对域名、时间、首个失败候选、
上游状态、本地策略键及 `observer_errors` / `dropped_events`，需要保留时再下载。
下载的是当前预览快照，不是完整任务记录、HAR、JD或登录材料，也不会自动发布到GitHub。
关闭诊断仅清除当前任务元数据。退出服务会丢失内存事件；开关偏好保留但下次是新 recording_id。

## 同机同网对照表（尚未执行，不是样例成绩）

| 项目 | 普通浏览器 | 现有HTTP桥 | D02原生路径 |
|---|---|---|---|
| 同一用户电脑/网络 | 待现场记录 | 待现场记录 | 待D02 |
| 日期/时间/时区 | 待记录 | 待记录 | 待D02 |
| 浏览器/OS/源码/适配器版本 | 待记录 | trace + PR head | 待D02 |
| 会话是否相同/有效 | 待记录混杂因素 | 待记录混杂因素 | 待D02 |
| 查询/地区/页数/获准范围 | 本地记录，不公开敏感值 | 同一条件 | 待D02 |
| 页面/列表/详情到达阶段 | 待观察 | 待观察 | 待D02 |
| 首个业务阻断候选及证据 | 待观察 | 脱敏trace | 待D02 |
| 完整目标JD数 | 未测试 | 未测试 | 未实现 |

仅在已确认正常访问条件下对照；近时间不等于相同会话或相同上游状态。
明确拒绝/挑战/冷却后停止，不为了填表不断刷新、切出口或换后端。
程序主动 abort 必须与平台 HTTP 响应分开；可选资源异常不能自动被称为岗位失败根因。
如果只有 `no_cards` 而没有明确业务成功的空响应，只能称没有识别到卡片，不称市场无岗位。

## G0、G1与关闭规则

#47 代码与受控验证齐备后可提交用户审查；真实现场证据仍独立保留为待验收。
#48 负责新后端及其最小控制；#49 才要求第一条真实相关完整JD进入现有报告。
不会因为下载了诊断JSON、合成测试产生了正文或有旧公开案例，就关闭 #47/#46/#9/#45。
PR 使用 Refs；不自动合并、发布或关闭原任务。
