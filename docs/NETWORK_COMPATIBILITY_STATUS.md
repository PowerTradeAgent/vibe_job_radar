# 网络兼容状态：已合并部分与第二阶段

## 已进入main

2026-09-15，用户要求已完成的功能先合并。PR #24已普通merge到main@a4f8e009f9cf03919d0a86b462e7047778e24de6。它交付：

- 只读系统/环境静态代理诊断，脱敏与NO_PROXY观察；发现不等于自动采用。
- 同次完整公网DNS快照、IPv4/IPv6交错、最多4个候选共享原超时；仅发送HTTP前切换。
- 显式匿名loopback HTTP CONNECT，变量VIBE_RADAR_HTTP_PROXY；HTTP、公开案例、搜索和Chromium网络桥实际共用。
- 原网站名SNI/TLS、代理失败不直连、HTTP/POST不重放、目标公网/域名/redirect/robots/配额保持。

用户授权的是上述有限范围，#23没有关闭。主干合并前重新跑399项本地测试通过；远端CI仍在执行步骤前失败，没有将本地结果冒充跨平台CI。docs/NETWORK_ROOT_CAUSE_REVIEW.md保留此前过程，属于历史归因，不是当前分支状态。

## 本次新PR：本机匿名SOCKS5

子任务 #25，从上述最新main开始。增加LoopbackSocks5、显式HTTP/SOCKS选择、原网络层接线、诊断协议回显和两种工作台的错误解释。详细操作见[本机SOCKS5](LOCAL_SOCKS5.md)。

仅接受本机SOCKS5匿名CONNECT和已通过原检查的IPv4/IPv6数字公网目标；不是socks5h、代理认证、UDP、远程入口或PAC。设置HTTP和SOCKS两个变量会直接拒绝，避免隐式走错路径。握手失败不直连，不发送任何源站请求；证书仍由原网络层校验。

新PR不修改系统DNS、TUN、VPN、虚拟机网络、路由或防火墙，不打开新服务端口。也没有重交此前被阻止的通用网络策略或Fake-IP绕行方案；该阶段是更小的、保留全部原安全条件的传输协议扩展。

## 剩余目标（都保留在#23）

| 目标 | 验收要求，未达到不标完成 |
|---|---|
| 系统代理自动采用 | 环境/系统/NO_PROXY与显式配置优先级清楚，实际请求走所选路线，失败不悄悄直连 |
| Fake-IP兼容 | 目标验证与代理映射分离；不能只是放开198.18.*或私网。正常失败与拒绝不被当作需要绕过的目标 |
| 代理认证 | 认证只到正确代理、密码不落盘/日志、不传给目标站、错误认证不循环尝试 |
| PAC | 不在应用进程任意执行脚本；系统求值或隔离机制、时限、目标选择和认证均需验证 |
| 网页网络设置 | 保存、回显与实际请求一致；后台/CLI策略一致、多工作区隔离，不能只加未接线按钮 |
| 虚拟机宿主机入口 | localhost语义准确、实际可达、明确授权入口；不扫描LAN或替用户开放防火墙 |
| 全矩阵验证 | 有代理/无代理、真实DNS/Fake-IP、Windows/Linux、正常/失效入口；真实取数和模拟测试分别记录 |

过去多次写入阻断没有消失，也不是只剩增加几行字段即可完成。因此不提供无依据的完成日期，不以原型或本地测试数承诺全场景可用。分期PR应给出明确实际功能与剩余边界。

## 验证说明

本次本地427项测试=main399+SOCKS阶段28。包含实际loopback SOCKS5和TLS服务器，不是仅mock函数返回；目标公网符号到本机服务器的映射只在测试服务器中存在。生产没有映射、信任覆盖或安全开关。最终CI状态另按新PR head回读。

正常系统路由通过TUN/VPN且DNS返回公网时，两种代理都不是必选；原系统路由保留。不论用哪一种路线，网站权限、网络断开、失效代理、证书错误、robots或HTTP拒绝都不能以“保证获取”伪装成功。
